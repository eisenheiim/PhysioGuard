// Importing the task definitions registers them with Render Workflows.
// The TypeScript SDK does not require an explicit app.start() call.
import "./tasks.js";

console.log("PhysioGuard Render Workflows registered");
